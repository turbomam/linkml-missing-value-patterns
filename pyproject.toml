[project]
name = "linkml-missing-value-patterns"
version = "0.1.0"
description = "Worked LinkML options for a slot that is mandatory and strongly typed but can carry a declared reason for absence"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "linkml>=1.9",
]

[tool.uv]
package = false
